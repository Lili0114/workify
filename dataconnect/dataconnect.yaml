specVersion: "v1beta"
serviceId: "workify"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "workify-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
